# SequentialMode

> `const` **SequentialMode**: [`PlacementMode`](../interfaces/PlacementMode.md) \<[`LinearPlacementContext`](../interfaces/LinearPlacementContext.md)\>

Keeps the session open after each confirmation so points can be chained.
