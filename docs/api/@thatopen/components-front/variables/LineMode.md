# LineMode

> `const` **LineMode**: [`PlacementMode`](../interfaces/PlacementMode.md) \<[`LinearPlacementContext`](../interfaces/LinearPlacementContext.md)\>

Clicking a projection line locks onto both its endpoints at once, then a second click sets the offset distance.
