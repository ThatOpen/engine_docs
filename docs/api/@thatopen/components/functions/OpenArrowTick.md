# OpenArrowTick()

> **OpenArrowTick**(`tip`, `lineDir`, `size`): `number`[]

Open-V arrowhead tick — two lines from the tip to the wing points, no base.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
