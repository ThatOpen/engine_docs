# getAngleTickEndpoints()

> **getAngleTickEndpoints**(`dim`): `object`[]

Returns the tip position and inward tangent direction for each tick endpoint of an angle dimension arc.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `dim` | `AngleAnnotation` |

## Returns

`object`[]
