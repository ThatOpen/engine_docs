# FilledCircleTick()

> **FilledCircleTick**(`tip`, `lineDir`, `size`): `number`[]

Filled circle tick (solid disc, requires a `THREE.Mesh`).

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
