# computeAngle()

> **computeAngle**(`dim`): `number`

Returns the angle in radians between the two rays defined by the dimension.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `dim` | `AngleAnnotation` |

## Returns

`number`
