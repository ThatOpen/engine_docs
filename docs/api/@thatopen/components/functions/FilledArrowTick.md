# FilledArrowTick()

> **FilledArrowTick**(`tip`, `lineDir`, `size`): `number`[]

Filled arrowhead tick (solid triangle, requires a `THREE.Mesh`).

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
