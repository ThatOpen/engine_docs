# computeBisectorAngle()

> **computeBisectorAngle**(`dim`): `number`

Returns the angle (in radians, in the XZ plane) of the bisector ray between the two measured rays.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `dim` | `AngleAnnotation` |

## Returns

`number`
