# FilledSquareTick()

> **FilledSquareTick**(`tip`, `lineDir`, `size`): `number`[]

Filled square tick (solid square, requires a `THREE.Mesh`).

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
