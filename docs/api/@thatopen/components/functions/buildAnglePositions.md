# buildAnglePositions()

> **buildAnglePositions**(`dim`, `style`): `number`[]

Builds the flat vertex positions for a single committed angle dimension.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `dim` | `AngleAnnotation` |
| `style` | `AngleAnnotationStyle` |

## Returns

`number`[]
