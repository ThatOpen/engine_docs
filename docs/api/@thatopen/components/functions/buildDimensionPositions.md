# buildDimensionPositions()

> **buildDimensionPositions**(`dim`, `style`): `number`[]

Builds the flat vertex positions (x,y,z triplets) for a single committed linear dimension.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `dim` | [`LinearAnnotation`](../interfaces/LinearAnnotation.md) |
| `style` | [`LinearAnnotationStyle`](../interfaces/LinearAnnotationStyle.md) |

## Returns

`number`[]
