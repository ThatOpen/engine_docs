# NoTick()

> **NoTick**(`tip`, `lineDir`, `size`): `number`[]

No tick — dimension line ends cleanly at the extension lines.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
