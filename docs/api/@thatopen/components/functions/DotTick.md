# DotTick()

> **DotTick**(`tip`, `lineDir`, `size`): `number`[]

Dot tick — a small circle drawn with line segments at the endpoint.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
