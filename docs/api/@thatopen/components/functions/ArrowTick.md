# ArrowTick()

> **ArrowTick**(`tip`, `lineDir`, `size`): `number`[]

Closed arrowhead tick — two wing lines plus a base line connecting them.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `tip` | `Vector3` |
| `lineDir` | `Vector3` |
| `size` | `number` |

## Returns

`number`[]
